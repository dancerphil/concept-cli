import {FileName, FileNameOfConcept, UsageListOfFileName} from './types';

const injectLink = (fileNames: FileName[], fileNameOfConcept: FileNameOfConcept) => {
    const usageListOfFileName: UsageListOfFileName = {}

    // for all concept with fileName as link
    // for all fileName in fileNames
    // find usage and replace usage to [conceptName](link)
    // and set links of file

    return usageListOfFileName
}

export default injectLink
